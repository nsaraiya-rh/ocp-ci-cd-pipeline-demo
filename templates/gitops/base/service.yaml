apiVersion: v1
kind: Service
metadata:
  name: sample-app
  labels:
    app.kubernetes.io/name: sample-app
    app.kubernetes.io/part-of: cicd-demo
spec:
  selector:
    app.kubernetes.io/name: sample-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
