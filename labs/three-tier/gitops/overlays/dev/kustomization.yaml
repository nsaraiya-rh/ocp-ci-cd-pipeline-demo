apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Dev overlay — lighter footprint (each of the 6 users runs their own copy in
# their own namespace).
resources:
  - ../../base

replicas:
  - name: frontend
    count: 1
  - name: api
    count: 1
